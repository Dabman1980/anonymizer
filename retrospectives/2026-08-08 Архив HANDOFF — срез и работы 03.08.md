# Архив HANDOFF — срез состояния и работы 03.08.2026

Вынесено из `HANDOFF.md` 08.08.2026 при закрытии сессии: точка входа переросла предел
в 200 строк. Ниже — **дословно**, без пересказа. Действующее (команда выката, грабли
`--exclude data`, `ufw limit`, проверка `surnames_status`) осталось в HANDOFF.

---

## Состояние

- **В бою:** ядро на `ru-vps-1` (`/srv/asya/anonymizer`, rsync-копия), Ася грузит его
  при старте. Выкачено 03.08.2026 14:19, 30 тестов на сервере зелёные, тесты Аси
  520 passed / 4 skipped, рестарт чистый, `asya`/`nl-socks`/`privoxy` активны.
  Пять выкатов за день, последний с 50 тестами на сервере и тестами Аси 520 passed.
  Проверено на сервере после рестарта:
  `…по адресу г. Тверь, пр-т Победы, д. 3` → `[АДРЕС_1]` ·
  `www.severdrev.ru` → `[САЙТ_1]`, ссылка с путём цела ·
  `Созвон завтра` цел · `Морозова прислала смету` → `[ФИО_1] прислала смету` ·
  `Роман с продолжением` и `Вера в успех` целы.
  ⚠️ **При выкате проверять, что словарный слой не молчит:** `surnames_status` должен
  быть «включён (pymorphy3)». Без пакета слой отключается БЕЗ ошибки, и утечка
  возвращается незаметно — поэтому проверка стоит перед рестартом и валит выкат.

**Команда выката (её не было в runbook Аси — там только комната `app`):**
```
cd ~/ClaudeProjects/real && rsync -az --delete --rsync-path="sudo rsync" \
  --exclude .git --exclude .venv --exclude __pycache__ --exclude .pytest_cache \
  --exclude .ruff_cache --exclude .DS_Store --exclude plans --exclude data \
  --exclude 'словарь_клиентов.txt' \
  anonymizer/ dmitry@5.129.195.59:/srv/asya/anonymizer/
```
⛔ **`--exclude data` обязателен.** 03.08 сухой прогон поймал, что без него на сервер
уехали бы три фотографии чужих визиток. `.gitignore` тут не помогает — **rsync его не
читает**. Это два разных контура утечки, и закрытие одного не закрывает другой.
⚠️ `--exclude 'словарь_клиентов.txt'` обязателен: без него `--delete` сотрёт клиентский
словарь на сервере. Перед настоящим прогоном — тот же вызов с `--dry-run --itemize-changes`
и счётом строк; ноль строк `deleting` = данные не пострадают.
⚠️ **Всю последовательность деплоя выполнять ОДНИМ ssh-подключением.** На сервере стоит
`ufw limit OpenSSH`: шесть подключений за 30 секунд — и порт начинает отвечать «Connection
refused». 03.08 это оборвало выкат ровно между rsync и рестартом, и Ася осталась на старом
ядре в памяти. Отличать от сбоя сервера просто: `ping` идёт, а порт 22 отказывает, —
значит лимит; лечится паузой в 2 минуты БЕЗ повторных попыток (каждая продлевает окно).
- **Синхронизировано 03.08.2026:** локальное дерево, `origin/main` и `/srv/asya/anonymizer`
  совпадают. Долг 16.07 (гомоглифы + скаффолд) закрыт тем же пушем; перед ним понадобилось
  перебазирование — на origin лежал коммит с картинкой примера в README.
  Пуш и деплой по-прежнему **только по команде Дмитрия**.
- **Тесты:** 50 (было 24). Слои: личный словарь → regex → NER (natasha) →
  словарный добор личных имён (pymorphy3, добавлен 03.08).

## Что сделано 03.08.2026

Пять правок за день, полный разбор с замерами и мутациями —
`retrospectives/2026-08-03 Разбор — три правки обезличивателя.md`. Коротко:

| Что | Итог |
| --- | --- |
| **Адрес без метки-с-двоеточием** | закрыт: шапка заявления, визитка, город из двух слов. Замер — синтетический корпус ДЗ PEd06 |
| **Домен раскрывает компанию** | закрыт узким вариантом: маскируется упоминание сайта, ссылка с путём цела. Различитель — **путь, а не схема** |
| **Переусердствование NER** | закрыто стоп-листом (+47 слов) с **машинной страховкой**: слово из списка не может оказаться фамилией |
| **Утечка: NER пропускает фамилию** | закрыта третьим проходом по словарю pymorphy3 |
| **Прогон по живому материалу** | закрыт пропуск имени на реальной визитке и переусердствование на реальных документах (масок 68 → 42) |

⛔ **Не переоткрывать без новых данных — замерено и отвергнуто:**
разборщик имён natasha (нужны словари pymorphy2, их нет) · нейроморфология
(«Созвон» и «Литвинов» одинаково `Animacy=Anim, POS=PROPN`) · правило
«неодушевлённое нарицательное» (сняло бы маскировку с 14 настоящих фамилий из 15) ·
широкое правило по `Surn` (маскирует «Роман с продолжением»).

**Прогон по ЖИВОМУ материалу (то, что раньше проверялось только синтетикой).**
9 реальных документов Дмитрия и 3 фотографии визиток; OCR — боевым путём Аси
(`exif_transpose` обязателен: без поворота Tesseract читает мусор, две карточки из трёх
давали ноль строк). В вывод не попало ни одного реального значения — только счётчики.

| Что проверялось | Итог |
| --- | --- |
| «имя первой строкой + должность второй» | **не воспроизвелось** на визитке, где форма есть: строка замаскирована |
| ⛔ но найдена другая утечка | на третьей визитке **не замаскировано НИ ОДНОГО ФИО**: личное имя в первой строке, NER не видит его вовсе, а словарный слой брал только фамилии |
| ⚠️ переусердствование | воспроизвелось массово: **68 масок на 7 финансовых документах, из них ~40 ложных** |

Правки: правило словарного слоя расширено до объединения «фамилия-не-имя ИЛИ все разборы
личные» (берёт имена и отчества, не трогает «Вера/Роман/Любовь» — у них есть нарицательное
чтение); добавлен признак «все разборы глагольные» против «Рассчитайте»/«Ответьте»;
в стоп-лист внесены финансовые термины и заголовочные слова.

⭐ **Гейт списка усилен, а не ослаблен.** Он отклонил «мсфо», «оквэд», «вебинар» — то есть
ровно те термины, ради которых заводился. Причина: у pymorphy3 есть **угадыватель**, и
незнакомое слово на «-о» он охотно объявляет фамилией. Замер: у настоящих фамилий личные
разборы **словарные** (`is_known`), у аббревиатур словарных личных разборов ноль. Гейт
теперь считает только словарные — ложные отказы ушли, запрет на настоящие фамилии остался
(проверено мутацией: подложенный «литвинов» краснит две проверки).

Результат на том же материале: масок **68 → 42**; обычных слов кириллицей 24 → 7 (из них
4 — артефакт склейки абзацев в моём извлекателе, а не дефект щита); аббревиатур 16 → 7;
на визитках пропущенных личных имён **ноль**. Тестов 50.

⚠️ Осталось: заголовки капсом («РЕЗЮМЕ», «КЕЙС») и «Общество/Общества» — существующее
правило для капса требует ещё и цифр. Не чинил: «ИВАНОВ» в шапке капсом — настоящая
фамилия, разделить их без замера нельзя.

⭐ **Три раза за день ломался тест, а не код:** проверка ссылки была зелёной по
случайной причине (зона `us` вне списка); класс тестов словарного слоя уходил в `skip`
на версии без слоя и рапортовал `OK`; фикстуры `make_anon`/`make_ner_anon` не включают
новый слой, и тест на нём ничего бы не проверял. Все три поймала мутация.

⚠️ **Дважды поправлял собственные выводы замером:** «переусердствование не
воспроизводится» (воспроизвелось живой пробой на сервере) и «женская фамилия в начале
фразы» (оказались точечные промахи на конкретных словах — 3 из 36 сочетаний).

