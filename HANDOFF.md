# HANDOFF — Anonymizer

Обновлено: 2026-08-08. Правила — в `CLAUDE.md` (читать вместе).
Статус: **ядро выкачено 08.08.2026 00:42 МСК**, на сервере 60 тестов ядра зелёных,
Ася перезапущена и активна, ошибок в журнале нет. `main` отправлен в `origin`.
Локально 90 тестов (60 ядра + 30 HTTP-режима).

⛔ **HTTP-режим НЕ запущен в бою намеренно.** Файлы `api.py`/`requirements-api.txt`
на сервер уехали вместе с деревом, но службы нет: systemd-юнит не ставился, порт не
слушается, fastapi в venv Аси не установлен. Решение о запуске — отдельное, после
замера под нагрузкой.

⚠️ **Тесты HTTP-режима видны не в каждом окружении.** В venv Аси их 30 в `skipped`
(fastapi там нет намеренно) — прогон рапортует `OK`, и это ровно та ловушка, на которой
проект уже обжигался. Полный прогон — отдельным venv с `requirements-api.txt`.

## ⭐ СЛЕДУЮЩИЙ ШАГ: ничего обязательного — работа по триггерам

Правки 07.08 в бою. Открытые вопросы, все по триггеру: запуск HTTP-режима (нужен второй
потребитель и замер под нагрузкой), второй режим «подъём абстракции» для 🟢-заметок,
устойчивость к повторному опознанию. Подробности — в карточке проекта в вольте.

## ⚠️ Выкат 08.08.2026: две находки про сам процесс

1. **`rsync -a --rsync-path="sudo rsync"` сменил владельца каталога** на сервере:
   было `asya:asya`, стало `dmitry:staff` (`-a` тащит владельца с Мака, а `sudo` даёт
   право это сделать). Ася читать могла — права 755, — но инвариант «владелец `asya`»
   был нарушен молча. Восстановлено `chown -R asya:asya` сразу после выката.
   ⭐ **Проверять владельца после каждого выката** — сам rsync об этом не скажет.
2. **Путь к venv в этом HANDOFF был записан неверно:** на сервере `/srv/asya/venv`,
   а не `/srv/asya/.venv`. И `/srv/asya` закрыт для пользователя `dmitry` — все проверки
   идут через `sudo -u asya bash -c "cd … && …"`, иначе `Permission denied`.
3. ⚠️ **Тесты Аси на сервере не проходят сбор:** `tests/test_poller_core.py` не находит
   модуль `poller` — каталога `poller` на сервере нет вовсе, хотя в локальном репозитории
   Аси он есть. Мой выкат касался только `/srv/asya/anonymizer` и удалил ноль файлов
   (сухой прогон подтвердил), так что это расхождение сервера с репозиторием Аси, а не
   следствие выката. ⛔ Но базовая линия её тестов ДО выката снята не была — строгого
   доказательства нет, только косвенное. Разобрать при следующей работе по Асе.

## Что сделано 07–08.08.2026 (подробности — в рефлексии)

Полный разбор с цифрами, мутациями и разбором моих ошибок —
`retrospectives/2026-08-08 Рефлексия — четыре задачи, два замера и три собственные ошибки.md`.

**✅ Падежные формы стоп-листа и «Общество».** Стоп-лист сверялся ТОЧНОЙ формой, поэтому
«Смету», «Отчёту», «Планёрку» уезжали в маску мимо списка. Теперь сверка идёт и падежной
формой (лемма pymorphy3) через единый `Anonymizer._stop_slovo`. Ложных тревог на своём
корпусе 8 → 3, recall на корпусе PEd06 не сдвинулся, сохранность 27 из 27.
⛔ **Страховок понадобилось ДВЕ, и одной мало.** Лемма настоящей фамилии попадает в
стоп-лист («Балансов» → «баланс»); признака «нет словарного личного разбора» не хватает —
словарь такой фамилии не знает вовсе. Спасает запрет на множественное число: перебор всех
1443 форм 150 слов дал 70 совпадений с формой фамилии, и ВСЕ 70 — множественное число.

**⛔ Заголовки капсом — гипотеза ОТВЕРГНУТА замером.** Различитель «маскировать капс только
при словарном (`is_known`) личном разборе» на оси «фамилия против заголовка» безошибочен
(34 слова, ноль промахов), но снимает маскировку с названий компаний капсом — 5 из 5
(«МТС», «ГАЗПРОМ», «АЭРОФЛОТ», «СЕВЕРДРЕВ», «РОМАШКА»); пропусков на корпусе-цели 4 → 6.
Причина принципиальная: словарь не отличает «АЭРОФЛОТ» от «РЕЗЮМЕ» ничем. Утечка дороже
переусердствования → заголовки капсом остаются лишней маской сознательно. Границу держит
тест `TestKapsNazvaniyaKompanij` — возврат гипотезы краснит его.
⚠️ Метрика слепа к коротким токенам: «МТС» (3 буквы) она считает замаскированным всегда
(значимый токен от 4 букв). Такие случаи проверять глазами.

**✅ HTTP-режим** — `api.py`, тонкая обёртка FastAPI над ядром: `POST /anonymize`,
`POST /restore`, `GET /health`. 30 тестов, `docs/http-rezhim.md`, `requirements-api.txt`.
Держится машинно: `/health` краснеет полем `ready:false` и кодом 503, если словарный слой
молчит · ключей на диске нет, в журнал уходят одни счётчики, наружу при сбое — класс
исключения без текста · fail-closed: нет слоя — отказ, нет токена — служба не стартует ·
состояние не течёт между запросами (слои общие, `Anonymizer` свой на запрос, модель под
замком). ⚠️ Пропускная способность не мерена. ⛔ В бою не запущен.

**Замер «подъём абстракции» — принят с оговоркой.** Единая линейка (судья `gemini-3.1-pro`,
прошёл оба гейта): маскирование — узнавание **10/10**, локальная `qwen2.5:7b` — 9/10,
`gpt-5-6-sol` через Kie — 5/10 и 7/10 при сохранности смысла 10/10.
⭐ **Про наш щит: он проваливает тест узнавания 10 из 10.** Имя ушло в `[ФИО_1]`, а город,
профессия и обстоятельство остались. Для 152-ФЗ не дефект (закон про идентификаторы), но
«обезличил — можно отправлять» этим не покрывается. Граница записана в `README.md`
разделом «Граница применимости».
⛔ **Приём не работает там, где нужен:** требует фронтир-модели, а 🔴-контур — ровно тот
случай, где облако закрыто. Значит это инструмент для 🟢-заметок, а не щит для чужих ПДн.
⛔ **Дефект замера, ставший было выводом:** из промпта выпала подстановка текста, модели
отвечали не видя заметки, и я объявил это свойством локальных моделей. Неверно. Поставлен
`assert "{text}" in PROMPT_ABSTRAKCIYA`. Стоимость прогонов — 13 кредитов Kie ≈ 6 ₽.

## Почему у проекта низкий приоритет в целом

Полный разбор — `plans/2026-07-16-nahodki-boevogo-progona.md` (локальный, в публикацию
не идёт). Единственный боевой потребитель — Ася, а она работает в
личном контуре Дмитрия, где **152-ФЗ не применяется** (ст. 1 ч. 2 п. 1 — обработка
физлицом для личных и семейных нужд). Значит щит там — **сверхдолжная мера**, а его
пропуски — вопрос гигиены, а не нарушение.

⚠️ **Что меняет приоритет на высокий:** через Асю пойдёт клиентский контур FinDir
(данные, пришедшие к Дмитрию как к консультанту). Тогда исключение отваливается,
включается ст. 12 (трансграничная передача), и щит становится обязательным. Граница
проходит по **цели обработки**, а не по типу данных: одна и та же визитка может быть с
обеих сторон границы.

## Состояние

- **В бою:** ядро на `ru-vps-1` (`/srv/asya/anonymizer`, rsync-копия), Ася грузит его
  при старте. Выкачено 08.08.2026 00:42 МСК: на сервере 60 тестов ядра зелёных, слои
  подняты, Ася перезапущена, ошибок в журнале нет. Локальное дерево, `origin/main` и
  сервер совпадают.
  ⚠️ **При выкате проверять, что словарный слой не молчит:** `surnames_status` должен
  быть «включён (pymorphy3)». Без пакета слой отключается БЕЗ ошибки, и утечка
  возвращается незаметно — поэтому проверка стоит перед рестартом и валит выкат.
  ⚠️ **И проверять владельца каталога** (`stat -c %U:%G`): `rsync -a` под sudo ставит
  владельца с Мака. Должен быть `asya:asya`.
- **Тесты:** 90 локально (60 ядра + 30 HTTP-режима). Слои: личный словарь → regex →
  NER (natasha) → словарный добор личных имён (pymorphy3).
  ⚠️ В venv Аси 30 тестов HTTP уходят в `skipped`, прогон рапортует `OK` — полный
  прогон только отдельным venv с `requirements-api.txt`.
- **Прошлые срезы 03.08 и работы того дня** — дословно в
  `retrospectives/2026-08-08 Архив HANDOFF — срез и работы 03.08.md`.

**Команда выката (её не было в runbook Аси — там только комната `app`):**
```
cd ~/ClaudeProjects/real && rsync -az --delete --rsync-path="sudo rsync" \
  --exclude .git --exclude .venv --exclude __pycache__ --exclude .pytest_cache \
  --exclude .ruff_cache --exclude .DS_Store --exclude plans --exclude data \
  --exclude 'словарь_клиентов.txt' \
  anonymizer/ dmitry@5.129.195.59:/srv/asya/anonymizer/
```
⛔ **`--exclude data` обязателен.** 03.08 сухой прогон поймал, что без него на сервер
уехали бы три фотографии чужих визиток. `.gitignore` тут не помогает — **rsync его не
читает**. Это два разных контура утечки, и закрытие одного не закрывает другой.
⚠️ `--exclude 'словарь_клиентов.txt'` обязателен: без него `--delete` сотрёт клиентский
словарь на сервере. Перед настоящим прогоном — тот же вызов с `--dry-run --itemize-changes`
и счётом строк; ноль строк `deleting` = данные не пострадают.
⚠️ **Всю последовательность деплоя выполнять ОДНИМ ssh-подключением.** На сервере стоит
`ufw limit OpenSSH`: шесть подключений за 30 секунд — и порт начинает отвечать «Connection
refused». 03.08 это оборвало выкат ровно между rsync и рестартом, и Ася осталась на старом
ядре в памяти. Отличать от сбоя сервера просто: `ping` идёт, а порт 22 отказывает, —
значит лимит; лечится паузой в 2 минуты БЕЗ повторных попыток (каждая продлевает окно).

## Что сделано 16.07.2026

Гомоглифы в `_ner_view` и скаффолд проекта. Вынесено дословно в
`retrospectives/2026-08-03 Архив HANDOFF — работы 16.07.md`.

## Известные пробелы (детали и триггеры — в плане)

- **Имя первой строкой + должность второй не ловится** — форма визитки. Чинится
  объединением спанов: целостный проход + построчный. Триггер: сканер визиток у Аси.
- ~~**Домен раскрывает компанию**~~ — **ЗАКРЫТ 03.08.2026 узким вариантом**
  (решение Дмитрия). Подробности в разделе «Что сделано» выше.
- ~~**Переусердствование по типу ФИО**~~ — **ЗАКРЫТО 03.08.2026** стоп-листом с машинной
  страховкой (разбор выше). ~~Осталось по типу ORG («Смету» → `[КОМПАНИЯ_1]`) и в падежных
  формах~~ — **ЗАКРЫТО 07.08.2026** падежной сверкой (разбор выше), в бой не выкачено.
  Осталось в этом классе: многословный спан («Уставный капитал Общества» уезжает целиком)
  и заголовки капсом — по ним гипотеза отвергнута замером, см. выше.
- **Пропуски NER, которые правка 07.08 НЕ чинит** (были и до неё): «Актов прислал
  документы» и «Клиентов согласовал смету» — NER не видит фамилию вовсе, а словарный
  слой её не знает; «КУЗНЕЦОВ АНДРЕЙ ВИКТОРОВИЧ» строкой визитки — капс мимо словарного
  слоя (`SLOVO` требует строчных); «СЕВЕРДРЕВ», «РОМАШКА» капсом — NER не даёт спана.
- ~~**HTTP-режим (FastAPI на RU-VPS)**~~ — **написан 07.08.2026**, 30 тестов, в бой
  не ставился. Остаток роадмапа — решение о выкате и замер под нагрузкой.
- **Маскирование не защищает от узнавания знакомым** — 10 из 10; ⭐ граница записана в
  `README.md` разделом «Граница применимости» 08.08.2026, чтобы результату щита не
  приписывали лишнего: корректно «прямые идентификаторы удалены», а не «человека не
  узнают». Два следствия заведены задачами в карточке вольта (второй режим под вторую
  задачу; устойчивость к повторному опознанию — оба по триггеру). (Замер 07.08, разбор
  выше). Не пробел щита в смысле 152-ФЗ, но граница его применимости, и она новая.

## Не переоткрывать

- **Словарь имён — отменён замером 16.07.** natasha берёт имена, включая одиночные;
  словарь чинил бы несуществующую болезнь и накрыл бы «Вера в успех», «Роман с
  продолжением», «Любовь и голуби», которые сейчас корректно не трогаются.
- **«Голая латиница не маскируется» — не дыра, а граница** (гвардия `replace`: нет
  кириллицы → NER не зовём). Русского текста без кириллицы не бывает. Зафиксировано
  тестом.
