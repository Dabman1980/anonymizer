# HANDOFF — Anonymizer

Обновлено: 2026-08-03. Правила — в `CLAUDE.md` (читать вместе).
Статус: 41 тест зелёный. Адрес, домен и переусердствование NER закрыты и **в бою**
(выкачено 03.08 в 15:07, тесты Аси 520 passed, рестарт чистый).
⛔ Открыта УТЕЧКА, найденная попутно: женская фамилия в начале фразы не маскируется.

## ⛔ СЛЕДУЮЩИЙ ШАГ: утечка — женская фамилия в начале фразы не маскируется

Найдено 03.08.2026 попутно, при разведке способов починить переусердствование.
**Это пропуск, а не лишняя маскировка, то есть дороже всего остального в этом файле.**

```
Морозова прислала смету   ->  Морозова прислала смету      ⛔ фамилия наружу
Морозов прислал смету     ->  [ФИО_1] прислал смету        ✅
Смету прислала Морозова   ->  [КОМПАНИЯ_1] прислала [ФИО_1] ✅
Позвони Морозовой         ->  [ФИО_1]                      ✅
```

Признак: женская фамилия в **именительном падеже в начале** короткой фразы. NER её
не видит вовсе — не «видит и фильтруется», а не находит спан. Не чинил: правка требует
собственного замера (сколько таких форм, не потянет ли за собой ложные), а сессия была
про другое. **Приоритет выше всех остальных пунктов ниже.**

⚠️ Там же видно вторую мелочь: «Смету» уехало в `[КОМПАНИЯ_1]` — переусердствование по
типу ORG. Стоп-лист его не ловит, потому что сверяет **точную форму** («смета» в списке
есть, «смету» нет). Это осознанная граница правки 03.08, а не недосмотр.

Переусердствование NER, бывшее следующим шагом до этого, **закрыто 03.08**:
`Созвон завтра` → `[ФИО_1] завтра` больше не воспроизводится. Разбор и три отвергнутых
способа — в «Что сделано 03.08.2026». Выкачено и проверено на сервере после рестарта.

## Почему у проекта низкий приоритет в целом

Полный разбор — `plans/2026-07-16-nahodki-boevogo-progona.md` (локальный, в публикацию
не идёт). Единственный боевой потребитель — Ася, а она работает в
личном контуре Дмитрия, где **152-ФЗ не применяется** (ст. 1 ч. 2 п. 1 — обработка
физлицом для личных и семейных нужд). Значит щит там — **сверхдолжная мера**, а его
пропуски — вопрос гигиены, а не нарушение.

⚠️ **Что меняет приоритет на высокий:** через Асю пойдёт клиентский контур FinDir
(данные, пришедшие к Дмитрию как к консультанту). Тогда исключение отваливается,
включается ст. 12 (трансграничная передача), и щит становится обязательным. Граница
проходит по **цели обработки**, а не по типу данных: одна и та же визитка может быть с
обеих сторон границы.

## Состояние

- **В бою:** ядро на `ru-vps-1` (`/srv/asya/anonymizer`, rsync-копия), Ася грузит его
  при старте. Выкачено 03.08.2026 14:19, 30 тестов на сервере зелёные, тесты Аси
  520 passed / 4 skipped, рестарт чистый, `asya`/`nl-socks`/`privoxy` активны.
  Три выката за день, последний 15:07 (41 тест на сервере, тесты Аси 520 passed).
  Проверено на сервере после рестарта:
  `…по адресу г. Тверь, пр-т Победы, д. 3, кв. 41` → `[АДРЕС_1]` ·
  `www.severdrev.ru` → `[САЙТ_1]`, ссылка с путём цела ·
  `Приёмка завтра, приедет Кузнецов` → `Приёмка завтра, приедет [ФИО_1]`.

**Команда выката (её не было в runbook Аси — там только комната `app`):**
```
cd ~/ClaudeProjects/real && rsync -az --delete --rsync-path="sudo rsync" \
  --exclude .git --exclude .venv --exclude __pycache__ --exclude .pytest_cache \
  --exclude .ruff_cache --exclude .DS_Store --exclude plans --exclude 'словарь_клиентов.txt' \
  anonymizer/ dmitry@5.129.195.59:/srv/asya/anonymizer/
```
⚠️ `--exclude 'словарь_клиентов.txt'` обязателен: без него `--delete` сотрёт клиентский
словарь на сервере. Перед настоящим прогоном — тот же вызов с `--dry-run --itemize-changes`
и счётом строк; ноль строк `deleting` = данные не пострадают.
⚠️ **Всю последовательность деплоя выполнять ОДНИМ ssh-подключением.** На сервере стоит
`ufw limit OpenSSH`: шесть подключений за 30 секунд — и порт начинает отвечать «Connection
refused». 03.08 это оборвало выкат ровно между rsync и рестартом, и Ася осталась на старом
ядре в памяти. Отличать от сбоя сервера просто: `ping` идёт, а порт 22 отказывает, —
значит лимит; лечится паузой в 2 минуты БЕЗ повторных попыток (каждая продлевает окно).
- **Синхронизировано 03.08.2026:** локальное дерево, `origin/main` и `/srv/asya/anonymizer`
  совпадают. Долг 16.07 (гомоглифы + скаффолд) закрыт тем же пушем; перед ним понадобилось
  перебазирование — на origin лежал коммит с картинкой примера в README.
  Пуш и деплой по-прежнему **только по команде Дмитрия**.
- **Тесты:** 41 (было 24). Слои: regex → NER (natasha) → личный словарь.

## Что сделано 03.08.2026

**Адрес без метки-с-двоеточием.** Замер на синтетическом корпусе из 12 текстов
(ДЗ PEd06, `~/ClaudeProjects/learning/ZeroCoder/PEd06/`) нашёл три формы, на которых
правило молчало, и все три — живой вход Аси:

| Форма | Почему молчало |
| --- | --- |
| `проживающей по адресу г. Тверь, пр-т Победы, д. 3, кв. 41` | «пр-т» не было в перечне типов улиц; меточное правило требует двоеточия, а в шапке заявления его нет |
| `Главный бухгалтер   г. Казань, ул. Лесная, 14` | номер дома без «д.», а группа требовала сокращение |
| `г. Нижний Новгород, ул. Большая Покровская, д. 18` | город из двух слов — захват обрывался на «Нижний» |

Расширен единственный паттерн «адрес без метки»: город из двух слов и через дефис,
типы улиц `пр-т/просп/б-р/мкр`, слово «город» целиком, голый номер дома **только
после распознанной улицы** (иначе шаблон начал бы хватать любые перечисления).

Шесть новых тестов. Пять из них **предъявлены прежней версии ядра** (`git show HEAD:`
в отдельном каталоге, `PYTHONDONTWRITEBYTECODE=1`) и там краснеют; шестой — анти-тест
«обычный текст с большой буквы не трогать» — зелёный на обеих версиях по построению,
и это заявлено, а не выдано за проверку.

**Домен — узкий вариант (решение Дмитрия 03.08).** Правило `website` маскирует
**только упоминание сайта**; ссылка на ресурс остаётся целой.

⚠️ **Различитель — наличие ПУТИ, а не схемы.** Эталонная ссылка проекта записана
без `https://` (`us06web.zoom.us/j/…`), и критерий «нет схемы» её бы не спас.
Зоны перечислены закрытым списком: открытый шаблон «слово.слово» превратил бы в сайт
любое сокращение с точкой. Зона `москва` в список **не входит сознательно** —
«г.Москва» без пробела стала бы доменом; собственные домены Дмитрия маскировать
незачем, это не чужие ПДн. Правило стоит ПОСЛЕ `email`, поэтому внутрь почты не лезет.

⭐ **Мутация вскрыла дефект в самом тесте.** Проверка «ссылка проходит щит целой»
была зелёной **по случайной причине**: у эталонной ссылки Zoom зона `us`, её нет в
списке зон, — то есть тест держался списком, а не стражем пути, и прошёл бы даже при
полностью сломанном страже. Обнаружено мутацией «сделать правило широким»: она
покрасила только соседний тест. Добавлена ссылка в зоне из списка
(`disk.yandex.ru/d/abc123`), после чего мутацию ловят обе проверки.

⚠️ **Поправка к моему же выводу, сделанная живой проверкой на сервере.** Сначала я
записал здесь, что переусердствование «не воспроизвелось ни разу» — на корпусе из трёх
текстов-ловушек («Вера в успех», «Роман с продолжением», «Май», «Учётная политика»)
ложных тревог действительно ноль. Но после выката пробный прогон на сервере дал
`Созвон завтра` → `[ФИО_1] завтра`. **Пробел живой, у меня просто не было в корпусе
фразы нужной формы.** Проверено на версии ДО обеих правок — поведение прежнее, мои
правки ни при чём.

Замер триггера (13 фраз): срабатывает не время, а **само слово**.

| Срабатывает | Чисто |
| --- | --- |
| `Созвон`, `Планёрка`, `Приёмка` + завтра / сегодня / послезавтра | `Отгрузка`, `Оплата`, `Встреча`, `Совещание`, `Договор` + завтра |
| `Созвон, завтра` | `Созвон в пятницу перенесли`, `созвон завтра` с малой буквы |

Похоже, редкое существительное с большой буквы в короткой фразе NER принимает за имя,
частотное — нет. **Практическая цена для Аси выше, чем «порча входа»:** это её типичный
вход (пересланное «Созвон завтра»), и после маскировки LLM извлекает событие, у которого
название заменено на `[ФИО_1]`.

Утечки тут нет — маскируется лишнее, а не пропускается нужное, поэтому в срочные не
переношу. Но формулировку «наблюдение» в списке пробелов ниже стоит считать устаревшей:
случай воспроизводится одной строкой.

⚠️ Второй пробел («имя первой строкой + должность второй») на корпусе действительно не
воспроизвёлся — визитку natasha взяла. Проверка на реальном материале — за Дмитрием.

**Переусердствование NER — закрыто списком, и это осознанный выбор из худших.**
Стоп-лист `NER_ROLE_STOPWORDS` дополнен 47 словами деловых событий (созвон, планёрка,
приёмка, отгрузка, смета…). Тестов 41.

⛔ **Три принципиальных способа замерены и отвергнуты — не переоткрывать без новых
данных:**

| Способ | Почему отвергнут |
| --- | --- |
| Разборщик имён `NamesExtractor` (natasha) | требует словарей pymorphy2, в среде их нет; тащить зависимость в модуль, живущий на сервере, — дороже задачи |
| Нейроморфология, признак одушевлённости | **не различает**: «Созвон» и «Литвинов» одинаково `Animacy=Anim`, `POS=PROPN`. Морфология повторяет решение той же модели, а не проверяет независимо |
| Словарь pymorphy3, правило «неодушевлённое нарицательное» | различает, но **перестал бы маскировать 14 настоящих фамилий из 15 проверенных** — Кисель, Борщ, Сахар, Топор, Молот, Гвоздь, Ключ, Мельница, Труба, Шило, Метель, Зима, Берёза, Сокол. Утечка дороже переусердствования |

⭐ **У списка есть машинная страховка.** Опасность стоп-листа в том, что одно неосторожное
слово превращает его в дыру: настоящая фамилия перестанет маскироваться. Поэтому
`test_stopwords_are_not_surnames` сверяет **каждое** слово списка со словарём pymorphy3 и
краснеет, если словарь знает его как имя/фамилию/отчество. Кандидат «вебинар» этой
проверкой уже отклонён — на глаз он выглядел безобидно.

Мутации предъявлены: список без новых слов → краснеет проверка поведения; подложенная
в список фамилия «литвинов» → краснеют **две независимые** проверки, поведенческая и
машинный гейт.

⚠️ Граница правки названа заранее: сверка идёт по **точной форме**, поэтому падежи
(«созвону», «смету») не покрыты. Нормализация потребовала бы pymorphy3 в самом модуле —
отдельное решение, сейчас не принималось.

## Что сделано 16.07.2026

Гомоглифы в `_ner_view` и скаффолд проекта. Вынесено дословно в
`retrospectives/2026-08-03 Архив HANDOFF — работы 16.07.md`.

## Известные пробелы (детали и триггеры — в плане)

- **Имя первой строкой + должность второй не ловится** — форма визитки. Чинится
  объединением спанов: целостный проход + построчный. Триггер: сканер визиток у Аси.
- ~~**Домен раскрывает компанию**~~ — **ЗАКРЫТ 03.08.2026 узким вариантом**
  (решение Дмитрия). Подробности в разделе «Что сделано» выше.
- ~~**Переусердствование по типу ФИО**~~ — **ЗАКРЫТО 03.08.2026** стоп-листом с машинной
  страховкой (разбор выше). Осталось по типу ORG («Смету» → `[КОМПАНИЯ_1]`) и в падежных
  формах: сверка идёт по точной форме слова.
- **HTTP-режим (FastAPI на RU-VPS)** — роадмап; тогда потребителей станет больше.

## Не переоткрывать

- **Словарь имён — отменён замером 16.07.** natasha берёт имена, включая одиночные;
  словарь чинил бы несуществующую болезнь и накрыл бы «Вера в успех», «Роман с
  продолжением», «Любовь и голуби», которые сейчас корректно не трогаются.
- **«Голая латиница не маскируется» — не дыра, а граница** (гвардия `replace`: нет
  кириллицы → NER не зовём). Русского текста без кириллицы не бывает. Зафиксировано
  тестом.
