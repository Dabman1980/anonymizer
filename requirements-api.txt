# Зависимости HTTP-режима (`api.py`). Ядру `anonymizer.py` они не нужны:
# кнопки и импорт модуля работают без единой строчки отсюда.
#
# Версии закреплены — те, на которых прогон зелёный (30 тестов API,
# живой прогон через uvicorn 07.08.2026).

fastapi==0.115.6
uvicorn[standard]==0.34.0

# Слои щита. Формально мягкие зависимости ядра, но для HTTP-режима
# обязательные: без них `/health` отдаёт `ready: false`, а `/anonymize`
# отказывает — служба не работает молча без слоя.
natasha==1.6.0
pymorphy3==2.0.6

# Только для тестов (`fastapi.testclient` ходит через httpx):
# httpx==0.28.1
